---
import type { HTMLAttributes } from "astro/types";
import Container from "../elements/Container.astro";
import Document from "../elements/Document.astro";
import Heading from "../elements/Heading.astro";
import Text from "../elements/Text.astro";

/** Long-form prose page. The default slot receives rendered MDX. */
type Props = HTMLAttributes<"section"> & {
  headline: string;
  subheadline?: string;
};

const { headline, subheadline, class: className, ...rest } = Astro.props;
---

<section class:list={["py-16", className]} {...rest}>
  <Container class="flex flex-col gap-10 sm:gap-16">
    <div class="flex flex-col items-center gap-6">
      <Heading class="max-w-5xl text-center">{headline}</Heading>
      {
        subheadline && (
          <Text size="lg" class="flex max-w-xl flex-col gap-4 text-center">
            <p>{subheadline}</p>
          </Text>
        )
      }
    </div>
    <Document class="mx-auto max-w-2xl">
      <slot />
    </Document>
  </Container>
</section>
